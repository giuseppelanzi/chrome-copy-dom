## Summary
[One plain line, max 132 characters, no line breaks and no formatting.
Say what the extension does and why it helps, in a single sentence.]

## Description
[Plain text only — the Chrome Web Store does NOT render Markdown.
Blank lines between paragraphs, emoji + UPPER CASE section headers on their own
line, "•" for bullets. No #, *, _, backticks or Markdown links (paste raw URLs).
Use the structure below, but fill it from the extension's real features and drop
any section that does not apply.]

📋 Easy Copy DOM — [short tagline]

[Intro: a hook question, then one or two sentences on what Easy Copy DOM is and the
problem it solves — grabbing the HTML of any element on a page without opening
DevTools and digging through the tree.]

🖱️ HOW IT WORKS
[Start the picker from the toolbar popup ("Copy now") or the keyboard shortcut.
Move the mouse to highlight any element — a label shows its tag, id and size —
click to copy its HTML to the clipboard, and press Esc to cancel. A toast confirms
what was copied.]

⌨️ KEYBOARD SHORTCUT
• Activate the picker: [default keys, with the Mac variant]
• Off by default — enable it in the popup, then set your own keys at
chrome://extensions/shortcuts
[Match the author's house style: key names in bold-math unicode, e.g.
𝐀𝐥𝐭 + 𝐒𝐡𝐢𝐟𝐭 + 𝐂.]

✨ KEY FEATURES
• [One-click copy of any element's full HTML (outerHTML).]
• [Live highlight with the element's tag, id and pixel size.]
• [Clean overlay isolated in a Shadow DOM — it never alters the page.]
• [Works on any normal web page.]
• [Two ways in — toolbar button or keyboard shortcut.]
• [Lightweight — injected only when you ask, nothing running in the background.]

⚙️ CUSTOMIZABLE
Open the popup from the toolbar icon to adjust:
• [Enable or disable the keyboard shortcut.]
• [Change the shortcut keys in Chrome settings.]

🔒 PRIVACY
[Runs entirely on-device. Uses only "storage" (to remember the shortcut toggle)
plus "activeTab" and "scripting" (to inject the picker into the current tab when
you trigger it). No host permissions, no data collection, no external
connections, no tracking.]

[Closing line: who it's for + a short call to action.]

💛 Support on Ko-fi: https://ko-fi.com/giuseppelanzi
🐙 Contribute or report an issue on GitHub: https://github.com/giuseppelanzi/chrome-copy-dom/
