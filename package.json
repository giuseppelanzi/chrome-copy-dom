{
  "name": "easy-copy-dom",
  "version": "1.0.1",
  "description": "Build tools for the Easy Copy DOM Chrome extension.",
  "private": true,
  "scripts": {
    "build": "node build-zip.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
